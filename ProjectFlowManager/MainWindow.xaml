<Window x:Class="ProjectFlowManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectFlowManager"
        mc:Ignorable="d"
        Title="Project Flow Manager" Height="500" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#95A5A6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16" Background="#F8F9FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель - Список проектов -->
        <StackPanel Grid.Column="0">
            <TextBlock Text="Projects" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,12"/>

            <Border Style="{StaticResource CardStyle}" Height="300">
                <ListBox ItemsSource="{Binding Projects}" 
                         SelectedItem="{Binding SelectedProject}" 
                         DisplayMemberPath="Name"
                         BorderThickness="0"
                         Background="Transparent">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#3498DB"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#EBF5FB"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                        Style="{StaticResource SecondaryButtonStyle}" Width="80"/>
                <Button Content="Add Project" Command="{Binding AddProjectCommand}" 
                        Style="{StaticResource PrimaryButtonStyle}" Width="100"/>
            </StackPanel>
        </StackPanel>

        <!-- Правая панель - Детали проекта -->
        <StackPanel Grid.Column="1" Margin="16,0,0,0">
            <TextBlock Text="Project Details" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,12"/>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <!-- Заголовок проекта -->
                    <TextBlock Text="{Binding SelectedProject.Name}" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Foreground="#2C3E50"
                               Margin="0,0,0,8"/>

                    <!-- Описание проекта -->
                    <TextBlock Text="{Binding SelectedProject.Description}" 
                               FontSize="12" 
                               Foreground="#7F8C8D" 
                               FontStyle="Italic"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <!-- Прогресс -->
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Progress" 
                                   FontWeight="SemiBold" 
                                   Foreground="#2C3E50"
                                   Margin="0,0,0,4"/>
                        <Grid>
                            <ProgressBar Value="{Binding SelectedProgress}" 
                                       Minimum="0" 
                                       Maximum="100" 
                                       Height="12"
                                       Background="#ECF0F1"
                                       BorderThickness="0">
                                <ProgressBar.Foreground>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#3498DB" Offset="0"/>
                                        <GradientStop Color="#2ECC71" Offset="1"/>
                                    </LinearGradientBrush>
                                </ProgressBar.Foreground>
                            </ProgressBar>
                            <TextBlock Text="{Binding SelectedProgress, StringFormat={}{0}%}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       FontSize="9"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                        </Grid>
                    </StackPanel>

                    <!-- Задачи -->
                    <TextBlock Text="Tasks" 
                               FontWeight="SemiBold" 
                               Foreground="#2C3E50"
                               Margin="0,0,0,8"/>

                    <Border BorderBrush="#ECF0F1" BorderThickness="1" CornerRadius="3">
                        <ItemsControl ItemsSource="{Binding SelectedProject.Tasks}" 
                                      BorderThickness="0"
                                      Background="White">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="8,6">
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}" 
                                                      Command="{Binding DataContext.ToggleTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                      CommandParameter="{Binding}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0">
                                                <CheckBox.Style>
                                                    <Style TargetType="CheckBox">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter Property="Foreground" Value="#2ECC71"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </CheckBox.Style>
                                            </CheckBox>
                                            <TextBlock Text="{Binding Title}" 
                                                       VerticalAlignment="Center" 
                                                       FontSize="12"
                                                       TextWrapping="Wrap"
                                                       Width="200"
                                                       HorizontalAlignment="Left">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                <Setter Property="Foreground" Value="#95A5A6"/>
                                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="False">
                                                                <Setter Property="Foreground" Value="#2C3E50"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Статистика -->
            <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Total Tasks: " FontWeight="SemiBold" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding SelectedProject.Tasks.Count}" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="Completed: " FontWeight="SemiBold" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding CompletedTasksCount}" FontWeight="Bold" Foreground="#2ECC71"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</Window>